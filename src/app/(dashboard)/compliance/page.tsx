import Link from "next/link";
import { Badge } from "@/components/ui/Badge";
import { Card } from "@/components/ui/Card";
import { Input } from "@/components/ui/Input";
import { PageHeader } from "@/components/ui/PageHeader";
import { Select } from "@/components/ui/Select";
import { listColleges, listPriorityGroups, listPrograms } from "@/server/repositories/reference-data.repository";
import { complianceReport } from "@/server/repositories/tracking.repository";

type ComplianceItem = { studentNumber: string; studentName: string; collegeName: string; programName: string; appointmentStatus: string; physicalExamStatus: string; laboratoryStatus: string };
export default async function CompliancePage({ searchParams }: { searchParams: Promise<Record<string, string | undefined>> }) { const params = await searchParams; const [report, colleges, programs, priorities] = await Promise.all([complianceReport({ collegeId: params.collegeId, programId: params.programId, priorityGroupId: params.priorityGroupId, physicalExamStatus: params.physicalExamStatus, laboratoryStatus: params.laboratoryStatus, appointmentStatus: params.appointmentStatus, search: params.search, page: 1, limit: 100, offset: 0 }), listColleges(), listPrograms(), listPriorityGroups()]); return <><PageHeader title="Compliance" description="Identify completed, pending, no-show, and follow-up students." /><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[["Students",report.summary.totalStudents],["Physical complete",report.summary.physicalCompleted],["Laboratory complete",report.summary.laboratoryCompleted],["Pending any",report.summary.pendingAny]].map(([label,value]) => <Card key={label}><p className="text-sm text-slate-500">{label}</p><p className="mt-2 text-3xl font-black">{value}</p></Card>)}</section><Card><form className="grid gap-3 md:grid-cols-3 xl:grid-cols-6"><Input name="search" defaultValue={params.search} placeholder="Student number or name" /><Select name="collegeId" defaultValue={params.collegeId}><option value="">All colleges</option>{colleges.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</Select><Select name="programId" defaultValue={params.programId}><option value="">All programs</option>{programs.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</Select><Select name="priorityGroupId" defaultValue={params.priorityGroupId}><option value="">All priorities</option>{priorities.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</Select><Select name="physicalExamStatus" defaultValue={params.physicalExamStatus}><option value="">Physical: any</option><option value="PENDING">Pending</option><option value="COMPLETED">Completed</option><option value="REQUIRES_FOLLOW_UP">Follow-up</option></Select><button className="rounded-lg border border-slate-300 font-bold">Filter</button></form></Card><Card className="overflow-hidden p-0"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50"><tr><th className="px-5 py-3">Student</th><th className="px-5 py-3">Appointment</th><th className="px-5 py-3">Physical</th><th className="px-5 py-3">Laboratory</th><th className="px-5 py-3"></th></tr></thead><tbody className="divide-y divide-slate-100">{(report.items as ComplianceItem[]).map((item) => <tr key={item.studentNumber}><td className="px-5 py-4"><p className="font-bold">{item.studentName}</p><p className="text-xs text-slate-500">{item.studentNumber} · {item.programName}</p></td><td className="px-5 py-4"><Status value={item.appointmentStatus} /></td><td className="px-5 py-4"><Status value={item.physicalExamStatus} /></td><td className="px-5 py-4"><Status value={item.laboratoryStatus} /></td><td className="px-5 py-4 text-right"><Link className="font-bold text-teal-700" href={`/students/${encodeURIComponent(item.studentNumber)}`}>View</Link></td></tr>)}</tbody></table></div></Card></>; }
function Status({ value }: { value: string }) { return <Badge tone={value === "COMPLETED" ? "success" : value === "NO_SHOW" ? "danger" : value === "REQUIRES_FOLLOW_UP" ? "warning" : "neutral"}>{value}</Badge>; }
